# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files (as they likely contain sensitive secrets)
*.tfvars
*.tfvars.json

# Ignore override files as they are usually environment/user specific
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# CLI plan files (can contain unencrypted sensitive values)
*.tfplan

# Lock file handling:
# Typically you DO want to commit .terraform.lock.hcl to lock provider versions.
# If you are building a generic/reusable Terraform module instead of root infrastructure,
# uncomment the line below to ignore it:
# .terraform.lock.hcl